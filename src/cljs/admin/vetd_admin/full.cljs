(ns vetd-admin.full
  (:require [vetd-app.hooks :as hks]
            [vetd-admin.admin :as admin]
            [vetd-app.util :as ut]   
            [secretary.core :as sec]))

(println "BEGIN FULL")

(println "END FULL")
