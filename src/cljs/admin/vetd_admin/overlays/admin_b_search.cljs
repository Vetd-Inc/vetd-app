(ns vetd-admin.overlays.admin-b-search
  (:require [vetd-app.flexer :as flx]   
            [reagent.core :as r]
            [re-frame.core :as rf]
            [re-com.core :as rc]))

(defn c-overlay []
  [:div "ADMIN BUYER SEARCH OVERLAY"])
